/**
 * PROJECT : FileName
 * ----------------------------------------------------------------------
 * Example of a react-native stylesheet.
 * 
 * @author    Developer <mail@mail>
 * @since     0.0.1
 */

// Imports
import { StyleSheet } from "react-native";

/**
 * Exported stylesheet.
 * 
 * @type {StyleSheet}
 */
const styles = StyleSheet.create({
  test: {
    backgroundColor: "#d3d7cf"
  }
});

// ----------------------------------------------------------------------
export default styles;
