/**
 * DummyTypes
 * ----------------------------------------------------------------------
 * Declara valores de constantes para tipos de ações a serem despachadas
 * para a store, nomes e valores devem ser únicos para evitar conflitos.
 *
 * Declares constant values for the action types to dispatch to the store,
 * names and values should be unique, to avoid conflicts.
 *
 * @since 0.0.1
 */

/**
 * Ação de teste "A".
 * Test action "A".
 *
 * @type {string}
 */
export const TEST_ACTION_A = "TEST_ACTION_A";

/**
 * Ação de teste "B".
 * Test action "B".
 *
 * @type {string}
 */
export const TEST_ACTION_B = "TEST_ACTION_B";
