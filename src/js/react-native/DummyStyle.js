import { StyleSheet } from "react-native";

/**
 * DummyStyle
 * ----------------------------------------------------------------------
 * Exemplo de stylesheet para React Native.
 * Example of a React Native stylesheet.
 * 
 * @since 0.0.1
 */
const Styles = StyleSheet.create({
  test: {
    backgroundColor: "#d3d7cf"
  }
});

// ----------------------------------------------------------------------

export default Styles;
